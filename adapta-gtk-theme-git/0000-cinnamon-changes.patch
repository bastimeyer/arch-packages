diff --git a/shell/sass/cinnamon/_common.scss b/shell/sass/cinnamon/_common.scss
index 58ab029a..e6310679 100644
--- a/shell/sass/cinnamon/_common.scss
+++ b/shell/sass/cinnamon/_common.scss
@@ -631,13 +631,13 @@ $ws_thumb_spacing: 32px;
     }
   }
 
-  &-overview-background-shade { background-color: opacity(black, $a_fg_3rd); }
+  &-overview-background-shade { background-color: rgba(0, 0, 0, 0); }
 }
 
 .workspace-close-button,
 .window-close {
-  height: 36px;
-  width: 36px;
+  height: 0;
+  width: 0;
   -cinnamon-close-overlap: 24px;
   background-image: url("assets/misc/close.svg");
   background-size: 36px;
@@ -1089,7 +1089,7 @@ $ws_thumb_spacing: 32px;
   &-subject {
     padding-top: 10px;
     padding-bottom: 6px;
-    
+
     &:ltr { padding-left: 17px; }
     &:rtl { padding-right: 17px; }
   }
@@ -1443,6 +1443,11 @@ $ws_thumb_spacing: 32px;
     &:highlight { background-color: $acc_fill_bg; }
   }
 
+  &-item-label {
+    min-width: 5px;
+    font-weight: 700;
+  }
+
   &-item-box {
     border: 0 none transparent;
     border-image: none;
@@ -1450,7 +1455,7 @@ $ws_thumb_spacing: 32px;
     background-color: $hidden_fill_bg;
     color: $osd_fg;
     box-shadow: $empty_shadow;
-    font-weight: 400;
+    font-weight: 700;
     &:hover {
       background-color: $osd_semi_fill_bg;
       color: if($variant != light, $sel_fg, black);
@@ -1492,7 +1497,7 @@ $ws_thumb_spacing: 32px;
     &.vertical StBin { padding: 0; }
 
     StLabel {
-      font-weight: 400;
+      font-weight: 700;
 
       .panel-top &,
       .panel-bottom & {
@@ -2000,12 +2005,9 @@ $bottom_gap: 4px;
 .expo {
   &-workspaces-name-entry {
     @include fontscaling($ref_size * 0.9);
-    -cinnamon-caption-spacing: 12px;
-    // FIXME: use 'height' property instead of min-height since total height
-    // affects to the size of vertically gap between thumbnails. And we can't
-    // use margins either. Foolish...
-    height: $ws_thumb_spacing - $top_gap - $bottom_gap;
-    padding: 0 8px;
+    -cinnamon-caption-spacing: 0;
+    height: 0;
+    padding: 10px 8px;
     border-radius: 0;
     border-top: $top_gap solid transparent; // add extra top gap
     border-bottom: $bottom_gap solid transparent; // add extra bottom gap
@@ -2035,10 +2037,7 @@ $bottom_gap: 4px;
     }
   }
 
-  &-background {
-    background-color: if($variant != light, opacity(black, $a_fg_3rd),
-                                            opacity(white, $a_fg_dis));
-  }
+  &-background { background-color: $bg; }
 }
 
 // /***********************
